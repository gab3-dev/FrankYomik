services:
  redis:
    image: redis:7-alpine
    volumes:
      - /home/akitaonrails/frank_yomik/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  init-perms:
    image: busybox
    env_file: /home/akitaonrails/frank_yomik/.env
    volumes:
      - /home/akitaonrails/frank_yomik/cache:/data/cache
    command: ["sh", "-c", "chown -R ${APP_UID:-1026}:${APP_GID:-1026} /data/cache"]
    restart: "no"

  api:
    image: 192.168.0.145:3007/akitaonrails/frank-yomik-api:latest
    env_file: /home/akitaonrails/frank_yomik/.env
    user: "${APP_UID:-1026}:${APP_GID:-1026}"
    ports:
      - "8080:8080"
    environment:
      REDIS_URL: redis://redis:6379
      AUTH_TOKEN: ${AUTH_TOKEN:?Set AUTH_TOKEN in .env}
      CACHE_DIR: /data/cache
    volumes:
      - /home/akitaonrails/frank_yomik/cache:/data/cache
    depends_on:
      redis:
        condition: service_healthy
      init-perms:
        condition: service_completed_successfully
    restart: unless-stopped

  worker:
    image: 192.168.0.145:3007/akitaonrails/frank-yomik-worker:latest
    env_file: /home/akitaonrails/frank_yomik/.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      APP_UID: ${APP_UID:-1026}
      APP_GID: ${APP_GID:-1026}
    command: ["--pipeline", "both", "--redis-url", "redis://redis:6379"]
    volumes:
      - /home/akitaonrails/frank_yomik/cache:/app/server/cache
      - /home/akitaonrails/frank_yomik/mangaocr-cache:/home/worker/.cache/manga_ocr
      - /home/akitaonrails/frank_yomik/huggingface-cache:/home/worker/.cache/huggingface
      - /home/akitaonrails/frank_yomik/easyocr-cache:/home/worker/.EasyOCR
      - /home/akitaonrails/frank_yomik/config.prod.yaml:/app/server/config.yaml:ro
    depends_on:
      redis:
        condition: service_healthy
      init-perms:
        condition: service_completed_successfully
    restart: unless-stopped

  init-cloudflared:
    image: busybox
    volumes:
      - /home/akitaonrails/frank_yomik/.cloudflared:/src:ro
      - cloudflared-config:/dest
    command: ["sh", "-c", "cp /src/* /dest/ && chmod 644 /dest/*"]
    restart: "no"

  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel --protocol http2 --edge-ip-version auto --config /etc/cloudflared/config.yml run yomik
    volumes:
      - cloudflared-config:/etc/cloudflared:ro
    depends_on:
      init-cloudflared:
        condition: service_completed_successfully
      api:
        condition: service_started
    restart: unless-stopped

volumes:
  cloudflared-config:
